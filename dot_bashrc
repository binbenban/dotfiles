# Load Homebrew environment (Linuxbrew)
if [ -d /home/linuxbrew/.linuxbrew/bin ]; then
  eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
fi

# Only switch to zsh for interactive shells
if [[ $- == *i* ]] && command -v zsh >/dev/null 2>&1; then
  exec zsh
fi

