ZSH_CONFIG_DIR="$HOME/.zsh"

for file in \
  history.zsh \
  path.zsh \
  env.zsh \
  completion.zsh \
  aliases.zsh \
  functions.zsh \
  keybindings.zsh \
  prompt.zsh \
  plugins.zsh
do
  [[ -f "$ZSH_CONFIG_DIR/$file" ]] && source "$ZSH_CONFIG_DIR/$file"
done
