ZSH_CONFIG_DIR="$HOME/.zsh"

for file in \
  history.zsh \
  path.zsh \
  env.zsh \
  keybindings.zsh \
  aliases.zsh \
  functions.zsh \
  completions.zsh \
  prompt.zsh \
  plugins.zsh \
  zoxide.zsh
do
  [[ -f "$ZSH_CONFIG_DIR/$file" ]] && source "$ZSH_CONFIG_DIR/$file"
done
